import { View } from "react-native";
import Layout from "./components/layout/layout";

const Category = () => {
  return (
    <Layout header={{ title: "Category" }}>
      <View></View>
    </Layout>
  );
};
export default Category;
